<section class="t m7">
   <div class="container">

    <ul class="list-menu">
      {{#each links}}
        <li><a href="{{target}}" class="list-menu__link">{{text}}</a></li>
      {{/each}}
    </ul>

  </div>
</section>